# CJdropshipping API credentials (https://developers.cjdropshipping.com/)
# Never commit real values - this file only documents the expected variables.
#
# Option A: paste the full key CJ gives you on the "Get API Key" page,
# in the form "<userNum>@api@<secret>" - it gets parsed automatically.
CJ_API_KEY_RAW=

# Option B: set email/password explicitly instead (takes precedence over A).
# CJ_EMAIL=
# CJ_PASSWORD=
